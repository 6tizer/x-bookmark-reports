---
description: 项目事实备忘（已对齐，勿重复求证）
alwaysApply: true
---

# 项目事实备忘（FACTS）

> 本文件记录已与用户对齐的关键事实。每次涉及相关话题时以此为准，**不要重复向用户求证**。
> 有新事实对齐后追加到此文件。

## F1. 归档内容早已在 Notion（2026-08-11 对齐）

- `output/归档/` 的 943 篇深度草稿对应的书签内容**早已全部上传过 Notion**（2026-04 起分批上传，如 4/1 批次 353 篇草稿模式上传后归档）。
- **本地状态文件 ≠ Notion 真实集合**：`.notion-upload-state.json`（draft 模式）2026-05-05 被清空；`.notion-finished-state.json` 2026-08-10 也重置过（存在 `.bak.empty.20260810` 备份）。本地 state 缺失不代表 Notion 里没有。
- 判断"是否已在 Notion"的唯一可靠方法：按 `文章链接`（source_url）查 Notion DB（`_check_duplicate_in_notion`）。
- 2026-08-11 实测：从 899 篇"pending"中抽样 8 篇，**8/8 已在 Notion**。
- 推论：任何"孤儿清理 / 补传 / 重跑"操作前，必须同时核对 `output/归档/` 与 Notion 既有记录，否则会触发几百篇规模的无意义重跑（见 BUGS.md B-RECONCILE-ARCHIVE-BLIND，2026-08-11 事故：942 篇重跑 19.5h，耗尽 xAI + Firecrawl 额度）。

## F2. 研究搜索栈的真实状态（2026-08-12 更新）

- SearXNG 实例（`http://100.99.184.51:8888`）跑在 **Tailscale 机器 tokyo-4-cqa**（SSH 别名 `tokyo`，`~/searxng-deploy/` docker compose，需 sudo）。该实例**属于 cryptoquantalpha 项目共用**，改配置须增量、勿动 news/crypto 引擎。
- 2026-08-12 已启用 general 引擎：**duckduckgo / brave / startpage / mojeek**（此前只有 google cse 一个，100 查询/天超限后 HTTP 200 + 0 结果，引发 08-11 Firecrawl 烧穿事故）。settings.yml 有 `.bak.20260812` 备份。
- Firecrawl 免费版 500 credits（一次性），`/v2/search` 每次 1 credit。代码防线（2026-08-12 加）：模块级熔断（402/429）+ `FIRECRAWL_MAX_CALLS_PER_RUN` 预算帽（默认 50）+ SearXNG 连续 5 次 0 结果 WARNING + health `degraded` 状态。
- xAI 团队额度已耗尽（403），用户未续费；研究主链路走 SearXNG，Exa /search 可选补充。

## F3. Notion 上传节奏（2026-08-11 对齐）

- `bin/upload_to_notion.py`：每篇 `sleep(3)` + 每 10 篇暂停 600s（`_BATCH_PAUSE_SECONDS`）；SKIP-DUP（查重跳过）也吃同样节奏 → 900 篇积压需 ~15h。
- Notion 官方限速 3 req/s，此节奏远超保守，可安全调低（见 BUGS.md B-UPLOAD-PACING-AMPLIFY）。

---
description: x-bookmark-reports 开发工作流规范
alwaysApply: true
---

# x-bookmark-reports 开发工作流规范

## 核心原则

1. **信息持久化**：所有重要发现必须写入文件（`BUGS.md`），不依赖 agent transcript
2. **Subagent 标准化**：每个 subagent 任务必须使用 `TASK_SUMMARY.md` 模板输出摘要
3. **状态跟踪**：Bug 状态（待修复 → 修复中 → 已修复）必须更新到 `BUGS.md`

---

## 工作流文件

| 文件 | 职责 |
|------|------|
| `PROJECT_CONTEXT.md` | 项目全局上下文（必读） |
| `BUGS.md` | Bug 跟踪（每次审查/修复后更新） |
| `TASK_SUMMARY.md` | Subagent 输出模板 |
| `WORKFLOW.md` | 工作流详细规范 |

---

## Subagent 调用规范

### 职责划分（重要）

| 谁来做 | 职责 |
|--------|------|
| **Subagent** | 执行任务、报告发现、输出摘要 |
| **主 agent** | 汇总发现、更新 BUGS.md、决策是否记录 |

**核心原则**：Subagent 只报告，主 agent 决策并执行文件更新。

### 任务描述必须包含

1. **任务目标**：明确要做什么
2. **上下文文件**：相关文件路径列表
3. **预期情况**：明确文件存在/不存在等边界情况
4. **BUGS.md 问题**：引用待修复的 BUG 编号（如有）
5. **验证方法**：如何验证成功
6. **输出格式**：使用 TASK_SUMMARY.md 模板

### 示例

```markdown
## 任务：修复 github_client.py 的 fallback 逻辑

### 上下文文件
- lib/github_client.py
- BUGS.md

### 问题
BUGS.md 中的 B001: `GitHubNotFound` 未被 fallback 捕获

### 验证
```bash
cd "/Users/tizer_mac_studio/work/Cursor/Twitter Bookmark/x-bookmark-reports"
python3 -c "from lib.github_client import GitHubClient; ..."
```

### 输出
按 TASK_SUMMARY.md 格式输出摘要。
```

---

## 审查流程

### 1. 调用审查 Subagent

```markdown
## 任务：审查 coordinator.py

### 预期情况
- lib/coordinator.py：已存在，需审查
- BUGS.md：已存在，需检查相关条目

### 审查清单
- [ ] URL 提取是否正确处理 dict 类型？
- [ ] Tweet ID 提取是否覆盖所有 URL 格式？
- [ ] ...

### 输出
按 TASK_SUMMARY.md 格式输出审查报告。
如有新发现，在"发现的新问题"中列出。
```

### 2. 汇总审查结果

审查完成后，**主 Agent 必须**：
1. 读取 subagent 输出的审查报告
2. 将发现的问题添加到 `BUGS.md` 的"待修复"表格
3. 更新问题状态为 `待修复`

**Subagent 不负责更新 BUGS.md，只报告发现。**

### 3. 修复并更新 BUGS.md

修复完成后，主 Agent 必须：
1. 将问题从"待修复"移到"已修复"表格
2. 填写修复日期和说明

---

## 验证流程

修复完成后，必须执行验证：

```bash
cd "/Users/tizer_mac_studio/work/Cursor/Twitter Bookmark/x-bookmark-reports"
python3 -c "
# 1. 导入测试
from lib.xxx import ...

# 2. 基本功能测试
client = XxxClient()
...

# 3. 边界条件测试
...

print('验证通过!')
"
```

---

## 严重性定义

| 级别 | 定义 | 处理优先级 |
|------|------|------------|
| HIGH | 致命错误，功能完全不可用 | 立即修复 |
| MEDIUM | 功能受限，但有 workaround | 尽快修复 |
| LOW | 小问题，不影响核心功能 | 计划修复 |

---

## 禁止行为

- 不要在 BUGS.md 之外记录 bug 状态
- 不要跳过验证直接声称"修复成功"
- 不要在 subagent 输出后不更新 BUGS.md
- 不要假设"用户应该知道"当前进度

---

## 参考

详细规范请见 `WORKFLOW.md`。
